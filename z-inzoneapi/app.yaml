runtime: python312

env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: /app/key.json

instance_class: F2
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 5

handlers:
- url: /.*
  script: auto

env: flex
